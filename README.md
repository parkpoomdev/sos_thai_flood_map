# SOS Flood Map - ระบบติดตามสถานการณ์น้ำท่วม

เว็บแอปพลิเคชันสำหรับแสดงแผนที่และกรองข้อมูล SOS Flood โดยใช้ Leaflet และ Web Workers สำหรับการประมวลผลแบบขนาน

## คุณสมบัติ

- 🗺️ **แผนที่แบบ Interactive** - แสดงจุดข้อมูลบนแผนที่ OpenStreetMap (70% ของหน้าจอ)
- 🔍 **ระบบกรองข้อมูล** - Panel กรองข้อมูลทางขวา (30% ของหน้าจอ)
- ⚡ **Parallel Processing** - ใช้ Web Workers สำหรับประมวลผลข้อมูลแบบขนาน
- 💾 **LocalStorage Cache** - เก็บข้อมูลใน cache เพื่อลดการดึงข้อมูลซ้ำ
- 📊 **สถิติแบบ Real-time** - แสดงสถิติข้อมูลแบบอัปเดตทันที
- 🚀 **Marker Clustering** - จัดกลุ่ม markers ที่ใกล้กันเพื่อเพิ่มประสิทธิภาพ
- 📈 **Progressive Rendering** - แสดง markers ทีละส่วนเพื่อไม่ให้ UI ค้าง
- ⏱️ **Debounce Filtering** - ลดการอัปเดตบ่อยเกินไปเมื่อกรองข้อมูล

## ระบบกรองข้อมูล

### 1. กรองตามสถานะการช่วยเหลือ
- `0`: รอการช่วยเหลือ
- `3`: กำลังดำเนินการช่วยเหลือ

### 2. กรองตามประเภทผู้ประสบภัย
- 👶 เด็ก
- 👴 ผู้สูงอายุ
- ♿ คนพิการ
- 👤 ทั่วไป

### 3. กรองตามตำบล (Subdistrict)
- เลือกได้หลายรายการ (กด Ctrl/Cmd ค้างไว้)
- มีตัวเลือก "ทั้งหมด" สำหรับแสดงทุกตำบล

## การติดตั้งและใช้งาน

### 1. เปิดไฟล์
เปิดไฟล์ `index.html` ในเว็บเบราว์เซอร์ (แนะนำ Chrome, Firefox, Edge)

### 2. ดึงข้อมูล
- ข้อมูลจะถูกดึงอัตโนมัติเมื่อเปิดหน้าเว็บ
- กดปุ่ม "🔄 รีเฟรช" เพื่อดึงข้อมูลใหม่

### 3. ใช้ Cache
- เปิดใช้งาน "ใช้ Cache (LocalStorage)" เพื่อเก็บข้อมูลไว้ในเครื่อง
- ข้อมูลจะถูก cache เป็นเวลา 5 นาที
- กด "รีเฟรช" เพื่อบังคับดึงข้อมูลใหม่
- กด "🗑️ ล้าง Cache" เพื่อล้างข้อมูลที่เก็บไว้

## โครงสร้างไฟล์

```
flood_info/
├── index.html          # หน้าเว็บหลัก
├── styles.css          # สไตล์และ CSS
├── app.js             # ตรรกะหลักของแอปพลิเคชัน
├── data-worker.js     # Web Worker สำหรับ parallel processing
└── README.md          # คู่มือการใช้งาน
```

## โครงสร้างข้อมูล

แอปพลิเคชันคาดหวังข้อมูลในรูปแบบ JSON ดังนี้:

```json
{
  "data": {
    "data": [
      {
        "_id": "...",
        "running_number": "...",
        "location": {
          "type": "Feature",
          "geometry": {
            "type": "Point",
            "coordinates": [longitude, latitude]
          },
          "properties": {
            "province": "...",
            "district": "...",
            "subdistrict": "...",
            "status": 0,
            "status_text": "...",
            "type": 1,
            "type_name": "...",
            "victims": ["เด็ก", "ผู้สูงอายุ"],
            ...
          }
        }
      }
    ]
  }
}
```

## เทคโนโลยีที่ใช้

- **Leaflet** - Library สำหรับแสดงแผนที่
- **Leaflet.markercluster** - สำหรับจัดกลุ่ม markers
- **Web Workers** - สำหรับ parallel processing
- **LocalStorage API** - สำหรับ cache ข้อมูล
- **Vanilla JavaScript** - ไม่ใช้ framework
- **requestAnimationFrame** - สำหรับ smooth rendering

## การทำงานของ Performance Optimizations

### Parallel Processing
1. ข้อมูลจะถูกแบ่งเป็น chunks (4 chunks)
2. แต่ละ chunk จะถูกประมวลผลใน Web Worker แยกกัน
3. ผลลัพธ์จะถูกรวมกันและส่งกลับไปยัง main thread
4. ช่วยให้ UI ไม่ถูกบล็อกระหว่างประมวลผลข้อมูลจำนวนมาก

### Marker Clustering
- ใช้ Leaflet.markercluster เพื่อจัดกลุ่ม markers ที่ใกล้กัน
- ลดจำนวน DOM elements ที่ต้อง render
- เพิ่มประสิทธิภาพเมื่อมี markers หลายพันจุด
- คลิกที่ cluster เพื่อ zoom in หรือ spiderfy

### Progressive Rendering
- แสดง markers ทีละ 100 จุดต่อ batch
- ใช้ requestAnimationFrame สำหรับ smooth rendering
- แสดง progress bar เมื่อมีข้อมูลมากกว่า 500 จุด
- UI ไม่ค้างแม้มีข้อมูลหลายพันรายการ

### Debounce Filtering
- หน่วงเวลา 300ms ก่อนอัปเดตเมื่อกรองข้อมูล
- ลดการ render ซ้ำๆ เมื่อผู้ใช้เปลี่ยน filter หลายครั้ง
- เพิ่มประสิทธิภาพและลดการใช้ CPU

## หมายเหตุ

### การรันแอปพลิเคชัน

**สำคัญ:** แอปพลิเคชันต้องรันผ่าน HTTP/HTTPS server (ไม่สามารถเปิดไฟล์โดยตรงได้ในบางเบราว์เซอร์)

#### วิธีที่ 1: ใช้ Python (แนะนำ)
```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000
```
แล้วเปิด `http://localhost:8000` ในเบราว์เซอร์

#### วิธีที่ 2: ใช้ Node.js (http-server)
```bash
# ติดตั้ง
npm install -g http-server

# รัน
http-server -p 8000
```

#### วิธีที่ 3: ใช้ VS Code Live Server
- ติดตั้ง extension "Live Server"
- คลิกขวาที่ `index.html` → "Open with Live Server"

### แก้ไขปัญหา Leaflet ไม่โหลด

หากพบข้อผิดพลาด `L is not defined` หรือ `ERR_ADDRESS_INVALID`:

1. **ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต** - Leaflet โหลดจาก CDN
2. **ใช้ HTTP Server** - อย่าเปิดไฟล์โดยตรง (file://)
3. **ลองรีเฟรชหน้าเว็บ** - อาจเป็นปัญหาเครือข่ายชั่วคราว
4. **ตรวจสอบ Console** - ดู error message เพิ่มเติม

### แก้ไขปัญหา LocalStorage QuotaExceededError

หากพบข้อผิดพลาด `QuotaExceededError: Failed to execute 'setItem' on 'Storage'`:

**สาเหตุ:** ข้อมูลมีขนาดใหญ่เกิน LocalStorage quota (ปกติประมาณ 5-10MB)

**วิธีแก้ไข:**

1. **ล้าง Cache** - กดปุ่ม "🗑️ ล้าง Cache" ในส่วนการตั้งค่า
2. **ปิดการใช้งาน Cache** - ติ๊กออกที่ "ใช้ Cache (LocalStorage)"
3. **ข้อมูลจะยังแสดงได้ปกติ** - แอปจะทำงานได้แม้ไม่เก็บ cache

**หมายเหตุ:**
- แอปจะตรวจสอบขนาดข้อมูลก่อนเก็บ (limit 4MB)
- ถ้าข้อมูลใหญ่เกิน จะข้ามการเก็บ cache อัตโนมัติ
- Cache จะหมดอายุใน 5 นาที

### CDN ที่ใช้

- Primary: `cdn.jsdelivr.net` (มี integrity check)
- Fallback: `unpkg.com`

## License

MIT License

